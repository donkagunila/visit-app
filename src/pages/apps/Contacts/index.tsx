import PageHeader from "../../../components/base/PageHeader";

const Contacts =  () => {
    return (
        <div>
            <PageHeader title={"Contacts"} className={"bg-white px-4 border-b border-slate-100"} />

            Contacts
        </div>
    );
}

export default Contacts