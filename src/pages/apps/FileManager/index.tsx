import PageHeader from "../../../components/base/PageHeader";

const FileManager = () => {
    return (
        <div>
            <PageHeader title={"File Manager"} className={"bg-white px-4 border-b border-slate-100"} />

            File Manager
        </div>
    );
};

export default FileManager;