import DataTable from "../../../../base/Table/DataTable";

const ProductionReport = () => {
    return (
        <div>
            <DataTable
                flat
                columns={[]}
                rows={[]}
                loading={false}>

            </DataTable>
        </div>
    );
};

export default ProductionReport;